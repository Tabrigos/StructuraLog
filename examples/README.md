## Esempio: Mock Log Producer

Per eseguire questo esempio, assicurati di aver installato la libreria in modalità 'editable' dal livello principale del repository:

```bash
pip install -e .
```

Quindi, esegui il producer:

```bash
python examples/mock_producer.py
```
